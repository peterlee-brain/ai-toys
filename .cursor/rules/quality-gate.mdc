---
description: 质量门禁 — 仓库级通用检查项
alwaysApply: true
---

# Quality Gate（仓库级）

AI 产出 **不算完成**，直到通过 **子项目门禁** + 下列通用项 + 人工确认。

## 通用（所有子项目）

- [ ] 只改了一个子项目；范围与 PLAN 当前步骤一致
- [ ] 无硬编码密钥、token、密码
- [ ] 无 `.env` 或凭证入库
- [ ] 规格/PLAN 已同步（若涉及接口或模型）

## 子项目验证

具体命令在 **子项目** `.cursor/rules/quality-gate.mdc`。

## Debug 格式

**预期 vs 实际** + **最小复现** + **文件路径**。

验证失败 **禁止** 标记步骤完成。
