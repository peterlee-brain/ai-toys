# Backend API base URL
VITE_API_BASE_URL=http://localhost:3000

# Optional: direct LLM key for local prototyping (prefer backend proxy in production)
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=

# Default context window (characters around selection)
VITE_CONTEXT_CHARS=120
